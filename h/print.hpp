#ifndef PRINT_HPP
#define PRINT_HPP

#include "../lib/hw.h"

extern void mprintString(char const* string);

extern void printInteger(uint64 xx);

//char* getStr(char *buf, int max);

//int strToInt(const char *s);

#endif